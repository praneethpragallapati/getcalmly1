// Marketing/landing route group — no shared header/footer chrome.
// The landing page is a faithful, self-contained port of the v2 brand mockup
// (it ships its own fixed nav, footer, and assessment modal).
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return children
}
